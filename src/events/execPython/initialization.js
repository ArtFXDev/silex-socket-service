const logger = require("../../utils/logger");


/**
 * /execPython initialization
 *
 * This initialization will launch the main loop of a socketio server
 * this loop listen for request and execute it
 */

const initialization = (socket, io) => {
    
}